import { state } from '@/components/Store/store';
import { useSnapshot } from 'valtio/react';

const Video1 = () => {
  return (
    <>
      {/* <Video
        ref={video}
        scale={[...aspect]}
        pos={[0, 0, -4]}
      /> */}
    </>
  );
};

export default Video1;
